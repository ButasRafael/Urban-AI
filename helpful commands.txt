
docker compose exec db psql -U postgres -d urban_ai   
\dt              -- list all tables in the current schema
\dn              -- list schemas
\c otherdb       -- connect to a different database
\d+ users        -- describe a table (columns, types, indexes)
SELECT * FROM users LIMIT 20;
\q               -- quit


# inside the running web container
docker compose exec web bash        # or `sh` on Alpine

# regenerate a clean first migration ONLY with your models
alembic revision --autogenerate -m "initial schema"

# inspect the file in alembic/versions/  ⇢  it should only contain CREATE TABLE for
#   users, revoked_tokens, whatever models you keep.  No DROP of postgis_* tables.

# then apply it
alembic upgrade head
exit

